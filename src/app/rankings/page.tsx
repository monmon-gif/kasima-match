import {RankingTable} from '@/components/RankingTable'; import {getActivePlayers} from '@/services/playerService'; import {getJ1LeagueMatches} from '@/services/matchService'; import {calculatePlayerStats} from '@/utils/calculatePlayerStats';
export const metadata={title:'選手ランキング'}; export default function RankingsPage(){const matches=getJ1LeagueMatches(2026),rows=getActivePlayers().map(player=>({player,stats:calculatePlayerStats(matches,player.playerId)}));return <><div className="page-head"><p className="eyebrow">PLAYER RANKING</p><h1>選手ランキング</h1><p>この表は2026/27明治安田J1リーグのみの個人成績です。</p></div><RankingTable rows={rows}/></>}
