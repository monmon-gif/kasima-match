import {RankingTable} from '@/components/RankingTable'; import {getActivePlayers} from '@/services/playerService'; import {getMatches} from '@/services/matchService'; import {calculatePlayerStats} from '@/utils/calculatePlayerStats';
export const metadata={title:'選手ランキング'}; export default function RankingsPage(){const matches=getMatches(),rows=getActivePlayers().map(player=>({player,stats:calculatePlayerStats(matches,player.playerId)}));return <><div className="page-head"><p className="eyebrow">PLAYER RANKING</p><h1>選手ランキング</h1><p>2026シーズン 個人成績</p></div><RankingTable rows={rows}/></>}
